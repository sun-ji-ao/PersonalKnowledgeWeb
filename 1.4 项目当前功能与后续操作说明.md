# 1.4 项目当前功能与后续操作说明

## 1. 文档说明

本文档基于以下资料和当前项目代码整理：

- `知识网站项目方案计划书.md`
- `1.3 项目代码架构设计文档.md`
- 当前 Astro 项目源码
- 当前 GitHub 推送后的项目状态

本文档用于说明：

- 当前已经实现了哪些功能。
- 当前尚未实现或后续可增强的功能。
- 后续本地开发、内容更新、构建、部署、域名绑定和维护操作。

## 2. 当前项目定位

当前项目是一个纯静态个人知识网站，不需要后端服务。

核心形态：

```text
bypassAV-study-main Markdown 文档
    |
    v
scripts/import_markdown.mjs 自动导入
    |
    v
Astro Content Collection
    |
    v
静态文章页 / 分类页 / 标签页 / 搜索索引
    |
    v
GitHub / Cloudflare Pages / 自有域名
```

当前技术栈：

| 模块 | 当前实现 |
| --- | --- |
| 网站框架 | Astro |
| 内容来源 | 仓库内 `bypassAV-study-main` |
| Markdown 渲染 | Astro Content Collection |
| 代码高亮 | Astro / Shiki |
| 全文搜索 | Pagefind + 本地标题索引兜底 |
| 样式 | `src/styles/global.css` |
| SEO | `src/components/SeoHead.astro` |
| 草稿规则 | `content_rules.json` |
| 部署方式 | 静态部署，推荐 Cloudflare Pages |
| 后端服务 | 当前不需要 |

## 3. 当前已实现功能

### 3.1 Markdown 自动导入

已实现。

相关文件：

```text
scripts/import_markdown.mjs
src/content.config.ts
```

功能说明：

- 从仓库内 `bypassAV-study-main` 读取全部 `.md` 文件。
- 自动复制到 `src/content/docs`。
- 自动补齐 frontmatter。
- 自动提取标题、分类、标签、排序。
- 自动规范化部分不兼容的代码块语言。
- 自动重写 Markdown 和 HTML 中的本地相对图片路径。
- 自动读取 `content_rules.json`，按规则生成 `draft` 状态。
- 自动删除与 frontmatter 标题重复的文章开头一级标题，避免文章页重复显示标题。
- 构建时自动执行导入。

当前构建脚本：

```json
"build": "npm run import && astro build && pagefind --site dist"
```

### 3.2 文章详情页

已实现。

相关文件：

```text
src/pages/docs/[...slug].astro
```

功能说明：

- 每篇 Markdown 自动生成一个文章页面。
- 显示文章标题、分类、标签、正文。
- 文章目录已从右侧栏移动到左侧栏下方。
- 文章目录不额外生成数字编号，仅按原始标题层级缩进。
- 支持上一篇 / 下一篇。
- 支持阅读进度条。
- 支持代码块复制。
- 支持返回顶部。

### 3.3 知识库索引页

已实现。

相关文件：

```text
src/pages/docs/index.astro
```

访问路径：

```text
/docs/
```

功能说明：

- 展示全部导入文章。
- 按分类分组。
- 显示文章数量。
- 支持跳转到 `/search/` 的站内搜索入口。
- 支持按关键词、分类、标签即时筛选文章。
- 支持筛选结果数量提示和空状态提示。
- 左侧分类入口会跳转到真实分类页，例如 `/categories/加密壳/`。
- 文章列表标题统一显示为 `1、文章标题`，不再显示 `课时 01` 形式。

### 3.4 首页

已实现。

相关文件：

```text
src/pages/index.astro
```

功能说明：

- 展示站点介绍。
- 展示搜索框，输入关键词后跳转到 `/search/` 页面集中展示结果。
- 展示知识分类。
- 展示标签云。
- 展示精选文章。
- 展示最新文章。
- 展示热门分类。
- 展示学习路线。
- 分类和标签入口指向真实分类页、标签页。
- 首页文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。
- 支持明暗主题切换。

### 3.5 分类系统

已实现。

相关文件：

```text
src/pages/categories/index.astro
src/pages/categories/[slug].astro
```

访问路径：

```text
/categories/
/categories/分类名/
```

功能说明：

- 展示全部分类。
- 展示每个分类下的文章。
- 分类来自 Markdown 一级目录。
- 分类文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。

### 3.6 标签系统

已实现。

相关文件：

```text
src/pages/tags/index.astro
src/pages/tags/[slug].astro
```

访问路径：

```text
/tags/
/tags/标签名/
```

功能说明：

- 展示全部标签。
- 展示每个标签下的文章。
- 标签由导入脚本根据分类和标题自动推断。
- 标签文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。

### 3.7 归档页

已实现。

相关文件：

```text
src/pages/archive/index.astro
```

访问路径：

```text
/archive/
```

功能说明：

- 按月份归档文章。
- 当前文章日期来自导入时生成的 frontmatter。
- 归档文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。

### 3.8 关于页

已实现。

相关文件：

```text
src/pages/about/index.astro
```

访问路径：

```text
/about/
```

功能说明：

- 说明站点定位。
- 说明内容规模。
- 说明技术栈。
- 说明内容更新方式。

### 3.9 全文搜索

已实现。

相关技术：

```text
Pagefind
本地标题索引兜底
```

相关文件：

```text
src/pages/search/index.astro
dist/pagefind/
```

访问路径：

```text
/search/?q=关键词
```

功能说明：

- 首页和知识库页的搜索框不再显示下拉结果。
- 输入关键词并点击“搜索”后跳转到 `/search/` 页面。
- 构建预览和线上环境优先使用 Pagefind 搜索正文。
- 本地 `npm run dev` 环境下使用内置索引兜底，搜索标题、摘要、分类和标签。
- 不需要后端搜索服务。

注意：

- Pagefind 对中文可用，但没有中文词干能力。
- 中文搜索更依赖文章标题、标签和摘要质量。
- `npm run dev` 不会生成 Pagefind 索引，因此开发预览时显示的是兜底索引结果。

### 3.10 RSS

已实现。

相关文件：

```text
src/pages/rss.xml.ts
```

访问路径：

```text
/rss.xml
```

功能说明：

- 输出最近文章 RSS。
- 供 RSS 阅读器订阅。

### 3.11 Sitemap

已实现。

相关文件：

```text
src/pages/sitemap.xml.ts
```

访问路径：

```text
/sitemap.xml
```

功能说明：

- 输出首页、知识库、搜索页、分类、标签、归档、关于和全部文章链接。
- 方便搜索引擎抓取。

### 3.12 robots.txt

已实现。

相关文件：

```text
src/pages/robots.txt.ts
```

访问路径：

```text
/robots.txt
```

功能说明：

- 允许搜索引擎抓取。
- 指向 sitemap。

### 3.13 404 页面

已实现。

相关文件：

```text
src/pages/404.astro
```

功能说明：

- 当访问不存在页面时，显示自定义 404 页面。
- 提供返回首页和进入知识库入口。

### 3.14 明暗主题切换

已实现。

相关文件：

```text
public/theme-toggle.js
src/styles/global.css
```

功能说明：

- 首页顶部按钮可切换主题。
- 其他页面右下角自动显示主题按钮。
- 使用 `localStorage` 记住用户选择。
- 支持深色和浅色主题。

### 3.15 阅读增强

已实现。

相关文件：

```text
public/reading-enhance.js
src/pages/docs/[...slug].astro
src/styles/global.css
```

功能说明：

- 文章阅读进度条。
- 代码块复制按钮。
- 返回顶部按钮。

### 3.16 图片路径自动修复

已实现。

相关文件：

```text
scripts/import_markdown.mjs
public/imported-images/
```

功能说明：

- 导入 Markdown 时扫描 `![](...)` 图片语法。
- 导入 Markdown 时扫描 HTML `<img src="">`。
- 对本地相对图片路径自动定位源文件。
- 自动复制图片到 `public/imported-images/`。
- 自动将文章中的图片地址重写为站点可访问路径。
- 外链、绝对路径、锚点和 data URI 不会被重写。

### 3.17 草稿和隐藏规则

已实现。

相关文件：

```text
content_rules.json
scripts/import_markdown.mjs
src/lib/content.ts
```

功能说明：

- `content_rules.json` 用于配置需要隐藏的目录、路径、分类、标题或标签。
- 导入脚本会根据规则写入 `draft: true`。
- `getPublishedDocs()` 会过滤草稿文章。
- 文章页、分类页、标签页、归档、RSS、sitemap 均基于公开文章生成。

当前默认规则：

```json
{
  "draftPathPrefixes": [],
  "draftPathIncludes": [],
  "draftCategories": [],
  "draftTitles": [],
  "draftTags": []
}
```

说明：默认不隐藏任何文章，只有明确写入规则后才会将文章标记为草稿。

### 3.18 SEO Open Graph

已实现。

相关文件：

```text
src/components/SeoHead.astro
public/og-image.svg
astro.config.mjs
```

功能说明：

- 主要页面统一输出 `title`、`description`、`canonical`。
- 支持 Open Graph：`og:title`、`og:description`、`og:type`、`og:url`、`og:image`。
- 支持 Twitter Card。
- 新增默认分享图 `public/og-image.svg`。
- 404 页面使用 `noindex`，避免被搜索引擎收录。

### 3.19 内容查询辅助函数

已实现。

相关文件：

```text
src/lib/content.ts
```

功能说明：

- 统一公开文章过滤逻辑。
- 统一分类排序逻辑。
- 提供最近文章查询。
- 提供精选文章查询。
- 提供分类、标签分组能力。
- 提供统一文章列表标题格式化能力，避免页面中继续显示 `课时 01` 形式。

### 3.20 页面展示问题修复

已实现。

相关文件：

```text
src/pages/docs/[...slug].astro
src/pages/docs/index.astro
src/pages/search/index.astro
src/lib/content.ts
src/styles/global.css
scripts/import_markdown.mjs
```

功能说明：

- 搜索框不再在输入时弹出下拉结果，统一跳转到 `/search/` 页面展示搜索结果。
- `/search/` 页面在 `npm run dev` 下使用内置索引兜底，避免本地开发时搜索结果为空。
- `/docs/` 左侧分类链接已改为真实分类页跳转。
- 文章列表中的 `课时 01`、`课时 02` 已统一改为 `1、`、`2、` 显示。
- 文章列表摘要已缩进，和标题形成清晰层级。
- 文章页标题区域已去掉重复摘要行。
- 导入脚本会删除正文开头与页面标题重复的一级标题。
- 文章目录已从右侧栏移动到左侧栏下方，并按标题层级缩进显示。

## 4. 当前项目代码结构

当前主要结构如下：

```text
website/
├─ astro.config.mjs
├─ package.json
├─ tsconfig.json
├─ content_rules.json
├─ bypassAV-study-main/
│  └─ 原始 Markdown 文档
├─ scripts/
│  └─ import_markdown.mjs
├─ public/
│  ├─ favicon.svg
│  ├─ og-image.svg
│  ├─ reading-enhance.js
│  ├─ theme-toggle.js
│  └─ imported-images/
├─ src/
│  ├─ components/
│  │  └─ SeoHead.astro
│  ├─ content.config.ts
│  ├─ lib/
│  │  └─ content.ts
│  ├─ pages/
│  │  ├─ index.astro
│  │  ├─ 404.astro
│  │  ├─ about/
│  │  ├─ archive/
│  │  ├─ categories/
│  │  ├─ docs/
│  │  ├─ search/
│  │  ├─ tags/
│  │  ├─ rss.xml.ts
│  │  ├─ sitemap.xml.ts
│  │  └─ robots.txt.ts
│  └─ styles/
│     └─ global.css
├─ 知识网站项目方案计划书.md
├─ 1.2 网站示意图/
├─ 1.3 项目代码架构设计文档.md
└─ 1.4 项目当前功能与后续操作说明.md
```

说明：

- `src/content/docs` 是构建时自动生成目录，已经加入 `.gitignore`。
- `dist` 是构建产物，已经加入 `.gitignore`。
- `public/imported-images` 是导入脚本生成的图片资源目录。
- `content_rules.json` 用于控制公开文章和草稿文章。
- GitHub 仓库中应提交源 Markdown、源码和脚本，不提交 `dist` 和 `node_modules`。

## 5. 当前未实现功能

以下功能目前还未实现，属于后续增强项。

### 5.1 部署配置文档

当前状态：未单独成文。

说明：

- 方案文档中有部署流程。
- 但还没有一个专门给 GitHub Pages / Cloudflare Pages 使用的部署操作文档。

建议后续实现：

- 新增 `1.5 部署上线操作手册.md`。
- 写清楚 GitHub Pages 配置。
- 写清楚 Cloudflare Pages 配置。
- 写清楚域名绑定和 DNS 配置。

### 5.2 域名绑定

当前状态：未在代码中配置真实域名。

说明：

- `astro.config.mjs` 当前默认：

```text
https://sun-ji-ao.github.io
```

- 可以通过环境变量 `SITE_URL` 覆盖。

建议后续操作：

- 确认最终域名。
- 在 Cloudflare Pages 中设置环境变量：

```text
SITE_URL=https://你的域名
```

### 5.3 内容质量优化

当前状态：持续优化项。

说明：

- 当前文章标题、分类、标签主要由导入脚本自动生成。
- 中文搜索效果依赖标题、摘要和标签质量。
- 原始 Markdown 中可能仍存在标题层级、代码块语言、图片资源缺失等内容问题。

建议后续操作：

- 人工优化重点文章标题和摘要。
- 为重点文章补充更准确的标签。
- 检查高价值文章中的图片、代码块和表格展示效果。

## 6. 常用本地操作

### 6.1 安装依赖

首次拉取项目后执行：

```bash
npm install
```

### 6.2 本地开发预览

```bash
npm run dev
```

默认访问：

```text
http://localhost:4321
```

常用页面：

```text
http://localhost:4321/
http://localhost:4321/docs/
http://localhost:4321/search/?q=hook
http://localhost:4321/categories/
http://localhost:4321/tags/
http://localhost:4321/archive/
http://localhost:4321/about/
```

### 6.3 手动导入 Markdown

```bash
npm run import
```

作用：

- 从 `bypassAV-study-main` 导入 Markdown。
- 生成 `src/content/docs`。
- 补齐 frontmatter。

### 6.4 构建静态站点

```bash
npm run build
```

构建流程：

```text
npm run import
    |
    v
astro build
    |
    v
pagefind --site dist
```

构建产物：

```text
dist/
```

### 6.5 本地预览构建产物

```bash
npm run preview
```

### 6.6 本地搜索说明

开发预览：

```bash
npm run dev
```

说明：

- `npm run dev` 不会生成 `dist/pagefind/`。
- `/search/` 页面会自动使用内置索引兜底。
- 兜底索引支持搜索标题、摘要、分类和标签。

完整搜索验证：

```bash
npm run build
npm run preview
```

说明：

- `npm run build` 会生成 Pagefind 索引。
- `npm run preview` 可以验证与线上更接近的正文全文搜索效果。

## 7. 内容更新流程

### 7.1 新增文章

推荐操作：

1. 把新的 `.md` 文件放入 `bypassAV-study-main` 对应目录。
2. 执行：

```bash
npm run build
```

3. 本地预览确认页面。
4. 提交并推送：

```bash
git add .
git commit -m "Update knowledge base content"
git push origin main
```

### 7.2 修改文章

推荐操作：

1. 修改 `bypassAV-study-main` 中的原始 Markdown。
2. 不要直接长期修改 `src/content/docs`。
3. 执行：

```bash
npm run build
```

4. 确认无构建错误后推送。

原因：

- `src/content/docs` 是导入生成目录。
- 每次构建会重新生成。

### 7.3 删除文章

推荐操作：

1. 删除 `bypassAV-study-main` 中对应 `.md` 文件。
2. 执行：

```bash
npm run build
```

3. 确认文章页面已消失。
4. 提交并推送。

### 7.4 隐藏文章或目录

推荐操作：

1. 修改 `content_rules.json`。
2. 按路径、分类、标题或标签添加隐藏规则。
3. 执行：

```bash
npm run build
```

4. 确认对应文章不再出现在文章页、分类页、标签页、归档、RSS 和 sitemap 中。

示例：

```json
{
  "draftPathPrefixes": ["20_免杀专题/"],
  "draftPathIncludes": [],
  "draftCategories": [],
  "draftTitles": [],
  "draftTags": []
}
```

字段说明：

| 字段 | 用途 |
| --- | --- |
| `draftPathPrefixes` | 按原始 Markdown 相对路径前缀隐藏 |
| `draftPathIncludes` | 按原始 Markdown 相对路径片段隐藏 |
| `draftCategories` | 按自动生成的分类名隐藏 |
| `draftTitles` | 按自动生成的文章标题隐藏 |
| `draftTags` | 按自动推断的标签隐藏 |

注意：不要直接修改 `src/content/docs` 中的 frontmatter，因为该目录会在导入时重新生成。

## 8. GitHub 推送流程

常用流程：

```bash
git status
git add .
git commit -m "提交说明"
git push origin main
```

如果推送失败：

- 检查 GitHub 网络连接。
- 检查代理/VPN。
- 检查远程地址：

```bash
git remote -v
```

当前远程仓库：

```text
https://github.com/sun-ji-ao/PersonalKnowledgeWeb.git
```

## 9. Cloudflare Pages 部署建议

推荐使用 Cloudflare Pages 部署。

配置项：

| 配置 | 值 |
| --- | --- |
| 构建命令 | `npm run build` |
| 输出目录 | `dist` |
| Node 版本 | 建议 20 或更新 |
| 环境变量 | `SITE_URL=https://你的域名` |

部署流程：

1. 登录 Cloudflare。
2. 进入 Pages。
3. 连接 GitHub 仓库。
4. 选择 `PersonalKnowledgeWeb`。
5. 设置构建命令：

```bash
npm run build
```

6. 设置输出目录：

```text
dist
```

7. 部署成功后绑定域名。

## 10. GitHub Pages 部署建议

也可以使用 GitHub Pages，但需要额外配置 GitHub Actions。

推荐方式：

- GitHub Actions 执行 `npm install`。
- GitHub Actions 执行 `npm run build`。
- 将 `dist` 发布到 GitHub Pages。

如果使用 Cloudflare Pages，则不需要 GitHub Pages。

## 11. 域名绑定操作

如果使用 Cloudflare Pages：

1. 在 Cloudflare Pages 项目中点击 Custom domains。
2. 添加你的域名，例如：

```text
www.yourdomain.com
```

3. 按 Cloudflare 提示添加 DNS 记录。
4. 设置环境变量：

```text
SITE_URL=https://www.yourdomain.com
```

5. 重新部署。

作用：

- RSS 链接会使用真实域名。
- sitemap 链接会使用真实域名。
- SEO 效果更稳定。

## 12. 当前构建结果参考

最近一次构建状态：

```text
Markdown 导入：250 篇
静态页面：290 个
Pagefind 索引：250 篇文章
构建结果：通过
```

已生成的关键路径：

```text
/
/docs/
/search/
/categories/
/tags/
/archive/
/about/
/rss.xml
/sitemap.xml
/robots.txt
/404.html
```

## 13. 后续推荐实现顺序

建议按以下顺序继续：

1. 新增 `1.5 部署上线操作手册.md`。
2. 确认最终域名并配置 `SITE_URL`。
3. 绑定 Cloudflare Pages 自定义域名。
4. 根据公开范围维护 `content_rules.json`。
5. 人工优化重点文章标题、摘要和标签。
6. 检查重点文章图片、表格和代码块显示效果。
7. 根据需要补充 GitHub Actions 或 Cloudflare Pages 部署说明。

## 14. 当前结论

当前网站已经具备个人技术知识库和技术博客的主要基础功能：

- 内容可以自动导入。
- 图片相对路径可以在导入时自动修复。
- 可以通过规则隐藏草稿或敏感文章。
- 页面可以静态生成。
- 文章可以通过 `/search/` 页面搜索。
- 分类、标签、归档、RSS、sitemap 已具备。
- `/docs/` 页面已支持前端即时筛选。
- 首页已具备真实文章信息流、热门分类和学习路线。
- 主要页面已具备 canonical、Open Graph 和 Twitter Card。
- 不需要后端服务。
- 可以直接部署到 Cloudflare Pages 或 GitHub Pages。

下一阶段重点不应再扩大架构，而应围绕以下方向优化：

- 内容质量。
- 部署文档。
- 域名绑定。
- 公开范围规则。
- 重点文章阅读体验。
