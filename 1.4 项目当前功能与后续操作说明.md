# 1.4 项目当前功能与后续操作说明

## 1. 文档说明

本文档基于以下资料和当前项目代码整理：

- `1.1 知识网站项目方案计划书.md`
- `1.3 项目代码架构设计文档.md`
- 当前 Astro 项目源码
- 当前 GitHub 推送后的项目状态

本文档用于说明：

- 当前已经实现了哪些功能。
- 当前尚未实现或后续可增强的功能。
- 后续本地开发、内容更新、构建、部署、域名绑定和维护操作。

## 2. 当前项目定位

当前项目是一个纯静态个人知识网站，不需要后端服务。

核心形态：

```text
bypassAV-study-main Markdown 文档
    |
    v
scripts/import_markdown.mjs 自动导入
    |
    v
Astro Content Collection
    |
    v
静态文章页 / 分类页 / 标签页 / 首页信息流 / 搜索索引
    |
    v
GitHub / Cloudflare Pages / 自有域名
```

当前技术栈：

| 模块 | 当前实现 |
| --- | --- |
| 网站框架 | Astro（自定义页面，未使用 Starlight） |
| 内容来源 | 仓库内 `bypassAV-study-main`（约 253 篇） |
| Markdown 渲染 | Astro Content Collection |
| 代码高亮 | Astro / Shiki |
| Mermaid 图表 | remark 插件 + 客户端渲染 |
| 全文搜索 | Pagefind + 本地标题索引兜底 |
| 样式 | `src/styles/global.css` |
| SEO | `src/components/SeoHead.astro` |
| 草稿规则 | `content_rules.json` |
| 日期持久化 | `content_dates.json` |
| 部署方式 | GitHub Pages + GitHub Actions（已上线） |
| 自定义域名 | `https://sunjiao.me`（阿里云 DNS，HTTPS 已启用） |
| 后端服务 | 当前不需要 |

## 3. 当前已实现功能

### 3.1 Markdown 自动导入

已实现。

相关文件：

```text
scripts/import_markdown.mjs
src/content.config.ts
```

功能说明：

- 从仓库内 `bypassAV-study-main` 读取全部 `.md` 文件。
- 自动复制到 `src/content/docs`。
- 自动补齐 frontmatter。
- 自动提取标题、分类、标签、排序。
- 支持两种文件名格式：`课时XX_标题.md` 与 `XX_标题.md`。
- 自动规范化部分不兼容的代码块语言。
- 自动重写 Markdown 和 HTML 中的本地相对图片路径。
- 自动读取 `content_rules.json`，按规则生成 `draft` 状态。
- 自动读取并更新 `content_dates.json`，区分历史文章与新增文章日期。
- 自动删除正文开头一级标题，避免文章页重复显示大标题。
- 自动为未编号的二级到四级 Markdown 标题补充 `1、`、`1.1、`、`1.1.1、` 形式编号。
- 已有编号的标题会保持原样，不会重复编号。
- 构建时自动执行导入。

当前构建脚本：

```json
"build": "npm run import && astro build && pagefind --site dist"
```

### 3.2 文章详情页

已实现。

相关文件：

```text
src/pages/docs/[...slug].astro
src/lib/content.ts
```

功能说明：

- 每篇 Markdown 自动生成一个文章页面。
- 显示文章标题（`编号、文章名`）、分类、标签、正文。
- 标题下方显示展示日期（历史文章为随机展示日期，新增文章为真实上传日期）。
- 左侧栏提供 `← 返回首页` 和 `← 返回知识库` 导航。
- 左侧栏分类名可点击，跳转到 `/categories/专题名/`。
- 文章目录位于左侧栏分类和标签信息下方。
- 「文章目录」标题单独一行固定；目录链接列表为独立滚动区域。
- 目录滚动条默认隐藏，鼠标移到目录列表区域时可用滚轮滚动。
- 正文标题在导入时统一补齐编号，文章目录读取同一份标题内容，保证正文和目录编号一致。
- 文章目录继续按标题层级缩进。
- 支持 Markdown 中的 Mermaid 流程图、时序图等图表渲染。
- 支持上一篇 / 下一篇。
- 支持阅读进度条。
- 支持代码块复制。
- 支持返回顶部。

### 3.3 知识库索引页

已实现。

相关文件：

```text
src/pages/docs/index.astro
```

访问路径：

```text
/docs/
```

功能说明：

- 展示全部导入文章。
- 按分类分组。
- 显示文章数量。
- 支持跳转到 `/search/` 的站内搜索入口。
- 支持按关键词、分类、标签即时筛选文章。
- 支持筛选结果数量提示和空状态提示。
- 左侧分类入口会跳转到真实分类页，例如 `/categories/加密壳/`。
- 文章列表标题统一显示为 `1、文章标题`，不再显示 `课时 01` 形式。

### 3.4 首页

已实现。

相关文件：

```text
src/pages/index.astro
src/pages/page/[page].astro
src/components/HomeShell.astro
src/components/HomeArticleFeed.astro
src/lib/content.ts
```

访问路径：

```text
/
/page/2/
/page/3/
...
```

功能说明：

- 采用左侧深色导航栏 + 右侧文章信息流的双栏布局。
- 左侧栏包含品牌、搜索框、分类专题、标签、归档、知识库、RSS、主题切换和分类列表。
- 右侧按展示日期倒序展示文章，每页 10 条。
- 每条文章显示标题（`编号、文章名`）、日期、可点击专题名、摘要。
- 底部分页显示 `第 X 页 / 共 Y 页`，支持上一页和下一页。
- 侧栏和文章区采用 SVG 线性图标与科技风样式。
- 支持明暗主题切换。

### 3.5 分类系统

已实现。

相关文件：

```text
src/pages/categories/index.astro
src/pages/categories/[slug].astro
```

访问路径：

```text
/categories/
/categories/分类名/
```

功能说明：

- 展示全部分类，按专题目录前缀序号（`01_`、`02_`…）排序。
- 展示每个分类下的文章，专题内按 `order` 排序。
- 分类来自 Markdown 一级目录。
- 分类文章列表统一显示为 `编号、文章名`，文章摘要缩进显示。

### 3.6 标签系统

已实现。

相关文件：

```text
src/pages/tags/index.astro
src/pages/tags/[slug].astro
```

访问路径：

```text
/tags/
/tags/标签名/
```

功能说明：

- 展示全部标签。
- 展示每个标签下的文章。
- 标签由导入脚本根据分类和标题自动推断。
- 标签文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。

### 3.7 归档页

已实现。

相关文件：

```text
src/pages/archive/index.astro
src/lib/content.ts
```

访问路径：

```text
/archive/
```

功能说明：

- 按展示日期归档文章，按月分组。
- 历史文章展示日期由 `buildDocDisplayDateMap()` 生成，范围覆盖 2021 年 11 月至 2025 年 10 月。
- 2026 年 5 月后新增文章按真实上传日期归档。
- 归档文章列表统一显示为 `1、文章标题`，文章摘要缩进显示。

### 3.8 全文搜索

已实现。

相关技术：

```text
Pagefind
本地标题索引兜底
```

相关文件：

```text
src/pages/search/index.astro
dist/pagefind/
```

访问路径：

```text
/search/?q=关键词
```

功能说明：

- 首页和知识库页的搜索框不再显示下拉结果。
- 输入关键词并点击“搜索”后跳转到 `/search/` 页面。
- 构建预览和线上环境优先使用 Pagefind 搜索正文。
- 本地 `npm run dev` 环境下使用内置索引兜底，搜索标题、摘要、分类和标签。
- 不需要后端搜索服务。

注意：

- Pagefind 对中文可用，但没有中文词干能力。
- 中文搜索更依赖文章标题、标签和摘要质量。
- `npm run dev` 不会生成 Pagefind 索引，因此开发预览时显示的是兜底索引结果。

### 3.9 RSS

已实现。

相关文件：

```text
src/pages/rss.xml.ts
```

访问路径：

```text
/rss.xml
```

功能说明：

- 输出最近文章 RSS。
- 供 RSS 阅读器订阅。

### 3.10 Sitemap

已实现。

相关文件：

```text
src/pages/sitemap.xml.ts
```

访问路径：

```text
/sitemap.xml
```

功能说明：

- 输出首页、知识库、搜索页、分类、标签、归档、分页首页和全部文章链接。
- 方便搜索引擎抓取。

### 3.11 robots.txt

已实现。

相关文件：

```text
src/pages/robots.txt.ts
```

访问路径：

```text
/robots.txt
```

功能说明：

- 允许搜索引擎抓取。
- 指向 sitemap。

### 3.12 404 页面

已实现。

相关文件：

```text
src/pages/404.astro
```

功能说明：

- 当访问不存在页面时，显示自定义 404 页面。
- 提供返回首页和进入知识库入口。

### 3.13 明暗主题切换

已实现。

相关文件：

```text
public/theme-toggle.js
src/styles/global.css
```

功能说明：

- 首页侧栏和其他页面均可切换主题。
- 其他页面右下角自动显示主题按钮。
- 使用 `localStorage` 记住用户选择。
- 支持深色和浅色主题。
- 新访客首次访问网站时默认使用 Dark 深色主题。
- 已手动切换过主题的用户继续使用本地保存的主题偏好。

### 3.14 阅读增强

已实现。

相关文件：

```text
public/reading-enhance.js
src/pages/docs/[...slug].astro
src/styles/global.css
```

功能说明：

- 文章阅读进度条。
- 代码块复制按钮。
- 返回顶部按钮。

### 3.15 图片路径自动修复

已实现。

相关文件：

```text
scripts/import_markdown.mjs
public/imported-images/
```

功能说明：

- 导入 Markdown 时扫描 `![](...)` 图片语法。
- 导入 Markdown 时扫描 HTML `<img src="">`。
- 对本地相对图片路径自动定位源文件。
- 自动复制图片到 `public/imported-images/`。
- 自动将文章中的图片地址重写为站点可访问路径。
- 外链、绝对路径、锚点和 data URI 不会被重写。

### 3.16 草稿和隐藏规则

已实现。

相关文件：

```text
content_rules.json
scripts/import_markdown.mjs
src/lib/content.ts
```

功能说明：

- `content_rules.json` 用于配置需要隐藏的目录、路径、分类、标题或标签。
- 导入脚本会根据规则写入 `draft: true`。
- `getPublishedDocs()` 会过滤草稿文章。
- 文章页、分类页、标签页、归档、RSS、sitemap 均基于公开文章生成。

当前默认规则：

```json
{
  "draftPathPrefixes": [],
  "draftPathIncludes": [],
  "draftCategories": [],
  "draftTitles": [],
  "draftTags": []
}
```

说明：默认不隐藏任何文章，只有明确写入规则后才会将文章标记为草稿。

### 3.17 SEO Open Graph

已实现。

相关文件：

```text
src/components/SeoHead.astro
public/og-image.svg
astro.config.mjs
```

功能说明：

- 主要页面统一输出 `title`、`description`、`canonical`。
- 支持 Open Graph：`og:title`、`og:description`、`og:type`、`og:url`、`og:image`。
- 支持 Twitter Card。
- 新增默认分享图 `public/og-image.svg`。
- 404 页面使用 `noindex`，避免被搜索引擎收录。

### 3.18 内容查询辅助函数

已实现。

相关文件：

```text
src/lib/content.ts
```

功能说明：

- 统一公开文章过滤逻辑。
- 统一分类排序逻辑（按专题目录序号）。
- 专题内文章按 `order` 排序。
- 提供 `getCategoryUrl()`，统一生成分类页链接。
- 提供展示日期生成、格式化和分页查询能力。
- 历史文章使用随机展示日期，新增文章使用真实上传日期。
- 提供按展示日期排序的文章列表查询。
- 提供分类、标签分组能力。
- 提供统一文章列表标题格式化能力，输出 `编号、文章名` 格式。
- 提供 Markdown 正文摘要提取能力，供首页信息流使用。

### 3.19 展示日期系统

已实现。

相关文件：

```text
src/lib/content.ts
src/pages/docs/[...slug].astro
src/pages/archive/index.astro
src/pages/index.astro
src/pages/page/[page].astro
```

功能说明：

- 文章页、首页、归档页统一使用展示日期。
- 历史文章（`useRealDate: false`）：展示日期随机分布在 2021 年 11 月 1 日至 2025 年 10 月 31 日。
- 新增文章（`useRealDate: true`）：展示日期等于 frontmatter 中的 `date`（真实上传日期）。
- 分界规则：2026 年 5 月 1 日及之后首次导入的文章记为新增文章。
- 同专题内按 `order` 递增，相邻文章间隔不超过 30 天，单专题跨度不超过 90 天。
- 年份分布偏向 2022、2023、2024，并保证每年每季度至少 1 篇文章。
- 单月文章上限为 20 篇。

相关文件：

```text
content_dates.json
scripts/import_markdown.mjs
```

### 3.20 页面展示问题修复

已实现。

相关文件：

```text
src/pages/docs/[...slug].astro
src/pages/docs/index.astro
src/pages/search/index.astro
src/lib/content.ts
src/styles/global.css
scripts/import_markdown.mjs
```

功能说明：

- 搜索框不再在输入时弹出下拉结果，统一跳转到 `/search/` 页面展示搜索结果。
- `/search/` 页面在 `npm run dev` 下使用内置索引兜底，避免本地开发时搜索结果为空。
- `/docs/` 左侧分类链接已改为真实分类页跳转。
- 文章列表中的 `课时 01`、`课时 02` 已统一改为 `1、`、`2、` 显示。
- 文章列表摘要已缩进，和标题形成清晰层级。
- 文章页标题区域已去掉重复摘要行。
- 文章页标题与分类页列表标题统一为 `编号、文章名` 格式。
- 导入脚本会无条件删除正文开头一级标题，避免详情页重复显示大标题。
- 首页信息流和文章详情页左侧分类名可点击，跳转到 `/categories/专题名/`。
- 分类页按专题目录序号排序，专题内文章按课时编号排序。
- 导入脚本支持 `课时XX_标题.md` 与 `XX_标题.md` 两种文件名格式。
- 导入脚本会为未编号正文标题补齐层级编号，已有编号标题保持原样。
- 文章目录已从右侧栏移动到左侧栏下方，并与正文标题编号保持一致。
- 文章侧栏目录支持独立滚动，滚动条隐藏，避免长目录挤占页面。

### 3.21 Mermaid 图表渲染

已实现。

相关文件：

```text
src/lib/remark-mermaid.mjs
src/lib/mermaid-client.ts
astro.config.mjs
src/pages/docs/[...slug].astro
src/styles/global.css
```

功能说明：

- Markdown 中的 ` ```mermaid ` 代码块会在构建时转为 `<div class="mermaid">`。
- 文章页加载后由客户端 `mermaid.run()` 渲染为 SVG。
- 兼容仍以普通代码块形式出现的 `language-mermaid` 内容，自动提升为图表容器。
- 支持流程图、时序图等常见 Mermaid 图形。
- 主题切换时可按明暗主题重新渲染。

依赖：

```text
mermaid
unist-util-visit
@astrojs/markdown-remark
```

## 4. 当前项目代码结构

当前主要结构如下：

```text
website/
├─ astro.config.mjs
├─ package.json
├─ tsconfig.json
├─ content_rules.json
├─ content_dates.json
├─ .github/
│  └─ workflows/
│     └─ deploy.yml
├─ bypassAV-study-main/
│  └─ 原始 Markdown 文档
├─ scripts/
│  └─ import_markdown.mjs
├─ public/
│  ├─ favicon.svg
│  ├─ og-image.svg
│  ├─ reading-enhance.js
│  ├─ theme-toggle.js
│  └─ imported-images/
├─ src/
│  ├─ components/
│  │  ├─ HomeShell.astro
│  │  ├─ HomeArticleFeed.astro
│  │  └─ SeoHead.astro
│  ├─ content.config.ts
│  ├─ lib/
│  │  ├─ content.ts
│  │  ├─ remark-mermaid.mjs
│  │  └─ mermaid-client.ts
│  ├─ pages/
│  │  ├─ index.astro
│  │  ├─ page/[page].astro
│  │  ├─ 404.astro
│  │  ├─ archive/
│  │  ├─ categories/
│  │  ├─ docs/
│  │  ├─ search/
│  │  ├─ tags/
│  │  ├─ rss.xml.ts
│  │  ├─ sitemap.xml.ts
│  │  └─ robots.txt.ts
│  └─ styles/
│     └─ global.css
├─ 1.1 知识网站项目方案计划书.md
├─ 1.2 网站示意图/
├─ 1.3 项目代码架构设计文档.md
├─ 1.4 项目当前功能与后续操作说明.md
└─ 1.5 部署上线操作手册.md
```

说明：

- `src/content/docs` 是构建时自动生成目录，已经加入 `.gitignore`。
- `dist` 是构建产物，已经加入 `.gitignore`。
- `public/imported-images` 是导入脚本生成的图片资源目录。
- `content_rules.json` 用于控制公开文章和草稿文章。
- `content_dates.json` 用于持久化文章日期和 `useRealDate` 状态，必须提交到 Git。
- GitHub 仓库中应提交源 Markdown、源码和脚本，不提交 `dist` 和 `node_modules`。

## 5. 当前未实现功能

以下功能目前还未实现，属于后续增强项。

### 5.1 内容质量优化

当前状态：持续优化项。

说明：

- 当前文章标题、分类、标签主要由导入脚本自动生成。
- 中文搜索效果依赖标题、摘要和标签质量。
- 原始 Markdown 中可能仍存在标题层级、代码块语言、图片资源缺失等内容问题。
- 含 Mermaid 的文章需在线上确认图表是否完整显示。

建议后续操作：

- 人工优化重点文章标题和摘要。
- 为重点文章补充更准确的标签。
- 检查高价值文章中的图片、代码块、表格和 Mermaid 图表展示效果。

### 5.2 阅读增强扩展

当前状态：可选后续项。

说明：

- Mermaid 已支持。
- 数学公式、Note / Tip / Warning 提示块、文件树组件等尚未实现。

### 5.3 已撤回的功能设计

以下能力已评估但明确不纳入当前版本：

- 文章上传页面。
- 在线 Markdown 编辑器。
- 在线图片上传与编辑。
- 在线代码块编辑。

原因：

- 当前站点是纯静态架构，没有后端服务和持久化存储。
- 内容发布继续采用 `bypassAV-study-main` + 导入脚本 + 静态构建流程。

## 6. 常用本地操作

### 6.1 安装依赖

首次拉取项目后执行：

```bash
npm install
```

### 6.2 本地开发预览

```bash
npm run dev
```

默认访问：

```text
http://localhost:4321
```

常用页面：

```text
http://localhost:4321/
http://localhost:4321/page/2/
http://localhost:4321/docs/
http://localhost:4321/search/?q=hook
http://localhost:4321/categories/
http://localhost:4321/tags/
http://localhost:4321/archive/
```

### 6.3 手动导入 Markdown

```bash
npm run import
```

作用：

- 从 `bypassAV-study-main` 导入 Markdown。
- 生成 `src/content/docs`。
- 补齐 frontmatter。

### 6.4 构建静态站点

```bash
npm run build
```

构建流程：

```text
npm run import
    |
    v
astro build
    |
    v
pagefind --site dist
```

构建产物：

```text
dist/
```

### 6.5 本地预览构建产物

```bash
npm run preview
```

### 6.6 本地搜索说明

开发预览：

```bash
npm run dev
```

说明：

- `npm run dev` 不会生成 `dist/pagefind/`。
- `/search/` 页面会自动使用内置索引兜底。
- 兜底索引支持搜索标题、摘要、分类和标签。

完整搜索验证：

```bash
npm run build
npm run preview
```

说明：

- `npm run build` 会生成 Pagefind 索引。
- `npm run preview` 可以验证与线上更接近的正文全文搜索效果。

## 7. 内容更新流程

### 7.1 新增文章

推荐操作：

1. 把新的 `.md` 文件放入 `bypassAV-study-main` 对应目录。
2. 文件名使用 `课时XX_标题.md` 或 `XX_标题.md` 格式。
3. 执行：

```bash
npm run build
```

3. 本地预览确认页面。
4. 提交并推送：

```bash
git add .
git commit -m "Update knowledge base content"
git push origin main
```

### 7.2 修改文章

推荐操作：

1. 修改 `bypassAV-study-main` 中的原始 Markdown。
2. 不要直接长期修改 `src/content/docs`。
3. 执行：

```bash
npm run build
```

4. 确认无构建错误后推送。

原因：

- `src/content/docs` 是导入生成目录。
- 每次构建会重新生成。

### 7.3 删除文章

推荐操作：

1. 删除 `bypassAV-study-main` 中对应 `.md` 文件。
2. 执行：

```bash
npm run build
```

3. 确认文章页面已消失。
4. 提交并推送。

### 7.4 隐藏文章或目录

推荐操作：

1. 修改 `content_rules.json`。
2. 按路径、分类、标题或标签添加隐藏规则。
3. 执行：

```bash
npm run build
```

4. 确认对应文章不再出现在文章页、分类页、标签页、归档、RSS 和 sitemap 中。

示例：

```json
{
  "draftPathPrefixes": ["20_免杀专题/"],
  "draftPathIncludes": [],
  "draftCategories": [],
  "draftTitles": [],
  "draftTags": []
}
```

字段说明：

| 字段 | 用途 |
| --- | --- |
| `draftPathPrefixes` | 按原始 Markdown 相对路径前缀隐藏 |
| `draftPathIncludes` | 按原始 Markdown 相对路径片段隐藏 |
| `draftCategories` | 按自动生成的分类名隐藏 |
| `draftTitles` | 按自动生成的文章标题隐藏 |
| `draftTags` | 按自动推断的标签隐藏 |

注意：不要直接修改 `src/content/docs` 中的 frontmatter，因为该目录会在导入时重新生成。

## 8. GitHub 推送流程

常用流程：

```bash
git status
git add .
git commit -m "提交说明"
git push origin main
```

如果推送失败：

- 检查 GitHub 网络连接。
- 检查代理/VPN。
- 检查远程地址：

```bash
git remote -v
```

当前远程仓库：

```text
https://github.com/sun-ji-ao/PersonalKnowledgeWeb.git
```

## 9. GitHub Pages 部署状态

线上部署已完成，日常只需推送 `main` 即可自动更新。

### 9.1 当前线上状态

| 项目 | 状态 |
| --- | --- |
| 线上地址 | `https://sunjiao.me` |
| 部署平台 | GitHub Pages |
| 自动部署 | 已启用（推送 `main` 触发） |
| HTTPS | 已启用 |
| 工作流 | `.github/workflows/deploy.yml` |

### 9.2 日常重新部署

```bash
git push origin main
```

或在 GitHub **Actions** 页签手动运行 `Deploy to GitHub Pages`。

详细配置、DNS 记录和排障说明见 `1.5 部署上线操作手册.md`。

### 9.3 部署配置项

| 配置 | 值 |
| --- | --- |
| 部署平台 | GitHub Pages |
| 部署方式 | GitHub Actions |
| 工作流文件 | `.github/workflows/deploy.yml` |
| 构建命令 | `npm run build` |
| 输出目录 | `dist` |
| Node 版本 | 22 |
| 环境变量 | `SITE_URL=https://sunjiao.me` |
| 自定义域名 | `sunjiao.me` |

## 10. Cloudflare Pages 备选方案

如需改用 Cloudflare Pages，可参考 `1.1 知识网站项目方案计划书.md` 中的备选部署说明。当前首选方案仍为 GitHub Pages + `sunjiao.me`。

## 11. 当前构建结果参考

最近一次构建状态：

```text
Markdown 导入：253 篇
静态页面：316+ 个
Pagefind 索引：253 篇文章
构建结果：通过
线上地址：https://sunjiao.me
```

已生成的关键路径：

```text
/
/page/2/
/docs/
/search/
/categories/
/tags/
/archive/
/rss.xml
/sitemap.xml
/robots.txt
/404.html
```

## 12. 后续推荐实现顺序

线上站点已可用。建议按以下顺序继续维护：

1. 根据公开范围维护 `content_rules.json`。
2. 人工优化重点文章标题、摘要和标签。
3. 检查重点文章图片、表格、代码块和 Mermaid 图表显示效果。
4. 按需补充数学公式、提示块等阅读增强能力。
5. 持续在 `bypassAV-study-main` 中新增或修订文章并推送上线。

## 13. 当前结论

当前网站已经具备个人技术知识库和技术博客的主要基础功能，并已完成线上部署：

- 内容可以自动导入（约 253 篇）。
- 正文标题可在导入时自动补齐层级编号。
- 图片相对路径可以在导入时自动修复。
- 可以通过规则隐藏草稿或敏感文章。
- 页面可以静态生成。
- 文章可以通过 `/search/` 页面搜索。
- 分类、标签、归档、RSS、sitemap 已具备。
- `/docs/` 页面已支持前端即时筛选。
- 分类按专题目录序号排序，文章标题统一为 `编号、文章名`。
- 首页和文章详情页的专题名可点击跳转分类页。
- 历史文章使用随机展示日期，2026年5月后新增文章使用真实上传日期。
- `content_dates.json` 已用于日期持久化，需随代码一并提交。
- 文章页已支持返回首页和返回知识库导航。
- 文章侧栏目录支持独立滚动，滚动条隐藏。
- Markdown Mermaid 图表可在文章页正确渲染。
- 新访客访问网站时默认进入 Dark 深色主题。
- 主要页面已具备 canonical、Open Graph 和 Twitter Card。
- 不需要后端服务。
- 已通过 GitHub Actions 部署到 GitHub Pages。
- 线上地址：`https://sunjiao.me`。

下一阶段重点：

- 内容质量优化。
- 公开范围规则维护。
- 重点文章阅读体验优化。
- 按需扩展数学公式等增强能力。
